// 导入vue
import Vue from 'vue'
// 导入VueRouter
import VueRouter from 'vue-router'
// use一下
Vue.use(VueRouter)

// 导入组件

// 路由规则
const routes = []

// 实例化路由对象
const router = new VueRouter({
  routes
})

// 暴露出去
export default router
